import React from "react";
import "./Button.css"

const Button = () => {
  return (
    <div>
      <button className="contact-btn">
        <a href="/contact-us">Contact</a>
      </button>
    </div>
  );
};

export default Button;
